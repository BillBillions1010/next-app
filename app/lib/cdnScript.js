import react from 'React'

/*/
///  JQueryBootStrapFont-Awesome.js
///  for kingBill.github.io by Billions.host
///  http://billions.host | billbillions.host
/*/
/*/ 
///  CDNs Included Here: 
/*     jQuery, BootStrap,
/*     React3Renderer, 
/*/
// cdnScript.js

const CdnScript = () => (
  <>
    {/* jQuery */}
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    {/* React */}
    <script src="https://unpkg.com/react@17/umd/react.development.js"></script>

    {/* ReactDOM */}
    <script src="https://unpkg.com/react-dom@17/umd/react-dom.development.js"></script>

    {/* Next.js */}
    <script src="https://unpkg.com/next@12/dist/next.js"></script>

    {/* Font Awesome */}
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"
      integrity="sha384-Z2lE3rZmZ3BKyHwaip2HztV6oCBE+aaU8FYM4bFcP8R+6Di5dm4Dw6wyXz8QKtT"
      crossorigin="anonymous"
    ></link>

    {/* JSX */}
    {/* Add your JSX CDN URL here */}

    {/* TypeScript */}
    {/* Add your TypeScript CDN URL here */}
  </>
);

export default CdnScript;

/* jquery.min 
import 'https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.min.js';
/* bootstrap.min 
import 'https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.0/js/bootstrap.min.js';
/* three.min 
import 'https://cdnjs.cloudflare.com/ajax/libs/three.js/r118/three.min.js';
/* React3Renderer 
import 'https://cdnjs.cloudflare.com/ajax/libs/react-three-renderer/3.2.4/React3Renderer.js';

export default function GetCdns() {
    const VendorCDN = document.getElementById('cdnDiv, VendorList'); 
    for(i=0; VendorList.length !==0, num in VendorList.length[i], i++;) {
        console.log(num);
        const listDiv = 
        document.appendChild(cdnDiv,
        return();
    };
    return(
        <div id=>
            <ul id='cdnDiv' className='justify-left col-2 shadow-lg list-block'>
                <li>{cdnList.name[1]}</li>
            </ul>
        </>
    );
}
const [second] = first 
export default function Load(second) {third}
*/
console.log("first: "+ CdnScript.value)